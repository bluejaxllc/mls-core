import puppeteer from 'puppeteer-extra'; import StealthPlugin from 'puppeteer-extra-plugin-stealth'; import fs from 'fs'; puppeteer.use(StealthPlugin()); (async () => { const browser = await puppeteer.launch({ headless: 'new' }); const page = await browser.newPage(); await page.goto('https://www.inmuebles24.com/casas-en-venta-en-chihuahua.html', { waitUntil: 'networkidle2' }); const html = await page.content(); fs.writeFileSync('i24.html', html); await browser.close(); console.log('Done'); })();
